#ifndef TEST_STUBS_H
#define TEST_STUBS_H

/*
 * The test suite needs access to the full Rogue type and macro definitions
 * so we simply include the primary header here.  This keeps the tests in sync
 * with the production sources and avoids hand-maintaining partial replicas.
 */
#include "../rogue.h"

#endif /* TEST_STUBS_H */
